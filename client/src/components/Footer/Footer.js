import React from 'react';
import styled from 'styled-components';

import styles from './Footer_style';

const Wrapper = styled.footer`${styles.wrapper}`;

const Footer = () => <Wrapper>Code4Fun</Wrapper>;

export default Footer;
