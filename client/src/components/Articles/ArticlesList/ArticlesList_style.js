import { css } from 'styled-components';
import { flex } from '../../../shared/styles';

const wrapper = css`
  ${flex}
  justify-content: space-around;
  padding: 1rem;
`;

export default {
  wrapper,
};
