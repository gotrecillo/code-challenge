import { css } from 'styled-components';
import { card } from '../../../shared/styles';

const wrapper = css`
  ${card}
  max-width: 1200px;
  margin: 0.5rem auto;
`;


export default {
  wrapper,
};
